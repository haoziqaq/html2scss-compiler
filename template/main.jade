mixin render(node)
    | #{String(node.clazz).includes(':tag') ? String(node.clazz).replace(':tag', '') : '.' + String(node.clazz)} {
    -if (node.children && node.children.length > 0)
        each item,index in node.children
            +render(item)
    | }
+render(tree)




