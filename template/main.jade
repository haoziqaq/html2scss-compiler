mixin render(node)
    | #{node.clazz.includes(':tag') ? node.clazz.replace(':tag', '') : '.' + node.clazz} {
    -if (node.children && node.children.length > 0)
        each item,index in node.children
            +render(item)
    | }
+render(tree)




